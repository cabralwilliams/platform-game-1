<g id="bgGroup{{groupNumber}}" data-scale="{{ dataScale }}" transform="scale({{ dataScale }})">
    {{#each backgroundObject}}
        <path d="{{ pathD }}" fill="{{ pathFill }}" opacity="{{ pathOpacity }}" />
    {{/each}}
</g>